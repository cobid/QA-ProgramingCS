int firstNumMax = int.Parse(Console.ReadLine());
int secondNumMax = int.Parse(Console.ReadLine());
int tirthNumMax = int.Parse(Console.ReadLine());
int secondNum = 0;

for (int i = 2; i <= firstNumMax; i+=2)
{
    for (int j = 1; j <= secondNumMax; j++)
    {
        
        for (int k = 2; k <= tirthNumMax; k+=2)
        {
            if (j == 2 || j == 3 || j == 5 || j == 7)
            {
                Console.WriteLine($"{i}{j}{k}");
            }
        }
    }
}
