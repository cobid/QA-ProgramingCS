int num = int.Parse(Console.ReadLine());
int nextNum = int.Parse(Console.ReadLine());
int nextNumValue = 0;

while (num != nextNum)
{
    nextNumValue = nextNum;
    nextNum = int.Parse(Console.ReadLine());
}
Console.WriteLine(nextNumValue * 1.2);
