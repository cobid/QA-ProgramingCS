int num = int.Parse(Console.ReadLine());
int number = 0;

for (int i = 1; i <= num; i++)
{
    for (int j = 1; j <= i; j++)
    {
        number++;
        Console.Write("{0} ", number);
        if (number == num)
        {
            return;
        }
    }
    Console.WriteLine();
}
